# Fibre Prettier Configuration

> [Prettier](https://prettier.io) config for all Fibre repositories.

## Usage

**Install**:

```bash
$ npm install -D git+https://github.com/fibre-scm/PrettierConfig.git
```

**Edit `package.json`**:

```jsonc
{
  // ...
  "prettier": "@fibre/prettier-config"
}
```
